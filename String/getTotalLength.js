// Write a function 'getTotalLength' that takes in 2 strings and returns the sum of the lengths of both strings

// function getTotalLength(str1, str2) {
//     return str1.length + str2.length;
// }

const getTotalLength = (str1, str2) => {
    return str1.length + str2.length;
}

console.log(getTotalLength("apple", "banana")); // 11 
console.log(getTotalLength("desktop", "laptops")) // 14
