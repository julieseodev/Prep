// function capitalizeWord(word) {
//     return word.toUpperCase();
// }

const capitalizeWord = (word) => {
    return word.toUpperCase();
}

console.log(capitalizeWord("julie")) // "JULIE"
console.log(capitalizeWord("peter")) // "PETER"
